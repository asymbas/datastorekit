# ``DataStoreKit``

Configure SwiftData with a custom data store.

## Overview

DataStoreKit integrates with SwiftData by providing a custom data store, such as its `DatabaseStore` that supports SQLite as its primary persistence layer.
